apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  namespace: cloudmart-prod
  labels:
    app: notification-service
spec:
  replicas: 1    # Single replica is fine — it's a queue consumer
  selector:
    matchLabels:
      app: notification-service
  template:
    metadata:
      labels:
        app: notification-service
    spec:
      containers:
      - name: notification-service
        image: <YOUR_REGISTRY>/notification-service:latest
        ports:
        - containerPort: 8004
        env:
        - name: PORT
          value: "8004"
        - name: ORDER_SERVICE_URL
          valueFrom:
            configMapKeyRef:
              name: cloudmart-config
              key: ORDER_SERVICE_URL
        - name: QUEUE_BACKEND
          valueFrom:
            configMapKeyRef:
              name: cloudmart-config
              key: NOTIFICATION_QUEUE_BACKEND
        - name: EMAIL_BACKEND
          valueFrom:
            configMapKeyRef:
              name: cloudmart-config
              key: NOTIFICATION_EMAIL_BACKEND
        - name: POLL_INTERVAL_MS
          valueFrom:
            configMapKeyRef:
              name: cloudmart-config
              key: NOTIFICATION_POLL_INTERVAL
        - name: AWS_REGION
          valueFrom:
            configMapKeyRef:
              name: cloudmart-config
              key: AWS_REGION
        - name: SQS_QUEUE_URL
          valueFrom:
            configMapKeyRef:
              name: cloudmart-config
              key: SQS_QUEUE_URL
        - name: FROM_EMAIL
          valueFrom:
            configMapKeyRef:
              name: cloudmart-config
              key: FROM_EMAIL
        - name: RECIPIENT_EMAIL_OVERRIDE
          valueFrom:
            configMapKeyRef:
              name: cloudmart-config
              key: RECIPIENT_EMAIL_OVERRIDE
        resources:
          requests:
            cpu: 50m
            memory: 64Mi
          limits:
            cpu: 200m
            memory: 128Mi
        readinessProbe:
          httpGet:
            path: /ready
            port: 8004
          initialDelaySeconds: 5
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /health
            port: 8004
          initialDelaySeconds: 10
          periodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: cloudmart-prod
spec:
  selector:
    app: notification-service
  ports:
  - port: 8004
    targetPort: 8004
  type: ClusterIP
